#ifndef SPLIT_ENTROPY_H
#define SPLIT_ENTROPY_H


class split_entropy
{
    public:
        split_entropy();
        virtual ~split_entropy();

    protected:

    private:
};

#endif // SPLIT_ENTROPY_H
